name: libsignal_dart
description: Dart bindings for the Signal protocol, built on libsignal's Rust core.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  ffi: ^2.1.3

dev_dependencies:
  lints: ^5.1.0
  test: ^1.25.0
